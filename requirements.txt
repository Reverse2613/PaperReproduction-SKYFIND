# ==========================================
# 核心深度学习框架 
# ==========================================
torch>=2.0.0          # 核心张量计算与自动求导 (依赖 CUDA 加速与 AMP/BF16)
torchvision>=0.15.0   # 图像基础变换 (transforms)、内置的 ResNet/DarkNet 残差思想来源

# ==========================================
# 图像处理与计算机视觉
# ==========================================
Pillow>=9.0.0         # 基础图像读取与画框 (dataset.py, demo.py, interactive.py)
opencv-python>=4.8.0  # 强大的矩阵级图像处理，用于将热力图叠加在原图上 (visualize_attention.py)

# ==========================================
# 自然语言处理 (NLP)
# ==========================================
transformers>=4.30.0  # 仅仅借用 HuggingFace 的分词器 (AutoTokenizer)，进行单词ID化

# ==========================================
# 数学计算与可视化
# ==========================================
numpy>=1.24.0         # CPU 端的矩阵桥梁，负责 PyTorch 张量与 OpenCV 图像的转换
matplotlib>=3.7.0     # 科学绘图基础库 (可视化热力图时的色彩映射依赖)

# ==========================================
# 训练工程辅助
# ==========================================
tqdm>=4.65.0          # 进度条，让自回归推理这种漫长的过程具有可观性